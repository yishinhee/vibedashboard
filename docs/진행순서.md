# harness-dashboard 진행 순서

> 이 프로젝트의 **현재 작업 단계**를 추적하는 단일 진실의 원천(Single Source of Truth).
> 매 작업 시작 직후 본 파일의 `현재 단계` 와 해당 단계 체크박스를 갱신한다.
> 단계 ID 표기법: `S{Phase}.{순번}` (예: `S0.3`, `S1.2`)

---

## 현재 단계

| 항목 | 값 |
|---|---|
| **현재 단계 ID** | `S1.10` (완료) → 다음: `S2.1` 진입 대기 |
| **현재 Phase** | Phase 1 (데이터 레이어) **완료** |
| **다음 Phase** | Phase 2 (프로젝트 관리 화면) |
| **마지막 갱신** | 2026-04-29 |
| **갱신 사유** | S1.10 완료 — sync_service 통합 테스트 8건 통과. **Phase 1 완료** (`manage.py test apps.parsers apps.sync` 38개 OK) |

> **갱신 규칙**: 단계가 시작·완료될 때마다 위 표 4개 행을 모두 업데이트한 후 다음 작업을 진행한다.
> CLAUDE.md §문서 갱신 규칙 + §Definition of Done 에서 강제된다.

---

## Phase 0 — 초기 세팅 ✅ 완료

| ID | 작업 | 산출물 | 상태 |
|---|---|---|---|
| S0.1 | CLAUDE.md / agents.template.md 검토 | (참조용) | ✅ |
| S0.2 | agents.md 작성 (Django 5.x + Tailwind, Redis·Celery 미사용) | `agents.md` | ✅ |
| S0.3 | 디렉토리 골격 생성 (`config/`, `apps/{projects,parsers,dashboard,sync}/`, `static/`, `templates/`, `tests/`, `docs/raw/`, `tmp/`) | 골격 디렉토리 | ✅ |
| S0.4 | docs/기능정의.md 정식 작성 (SCR 7, F 22, 테이블 9, Phase 5단계) | `docs/기능정의.md` | ✅ |
| S0.5 | docs/feature_map.md, docs/프로그램구조.md, docs/uiux개선.md, docs/프로젝트설계.md 골격 작성 | docs 4종 | ✅ |
| S0.6 | tmp/telegram_pending.txt 작성 (Stop hook 발송 대상) | `tmp/telegram_pending.txt` | ✅ |
| S0.7 | .env / .env.example / .gitignore 작성 (비밀값 .env 단일화) | `.env`, `.env.example`, `.gitignore` | ✅ |

---

## Phase 1 — 데이터 레이어 ✅ 완료

> 외부 프로젝트의 docs/*.md 를 파싱해 SQLite 에 저장하는 백엔드 완성.

| ID | 작업 | 산출물 | 상태 |
|---|---|---|---|
| S1.1 | Django 프로젝트 초기화 (`django-admin startproject config .`) + `requirements.txt` 작성 | `manage.py`, `config/__init__.py`, `requirements.txt` | ✅ |
| S1.2 | settings 분리 (`config/settings/base.py`, `dev.py`, `prod.py`) + `.env` 로드 (`python-decouple` 또는 `django-environ`) | `config/settings/*.py` | ✅ |
| S1.3 | 4개 앱 등록 (`apps.projects`, `apps.parsers`, `apps.dashboard`, `apps.sync`) + `apps.py` 생성 | `apps/*/apps.py`, INSTALLED_APPS 갱신 | ✅ |
| S1.4 | 모델 9종 정의 (`Project`, `Screen`, `Feature`, `AppModule`, `ModelInfo`, `Phase`, `ChecklistItem`, `ImprovementItem`, `FeatureMap`, `SyncLog`) | `apps/*/models.py` | ✅ |
| S1.5 | 마이그레이션 생성·적용 (`makemigrations` → `migrate`) | `apps/*/migrations/0001_*.py`, `db.sqlite3` | ✅ |
| S1.6 | `BaseParser` 추상 클래스 + `PARSER_REGISTRY` | `apps/parsers/services/base_parser.py`, `__init__.py` | ✅ |
| S1.7 | 파서 5종 구현 (feature/structure/improvement/design/feature_map) | `apps/parsers/services/*_parser.py` | ✅ |
| S1.8 | `sync_service.sync_one_file` (mtime 비교 + SyncLog 기록) | `apps/sync/services/sync_service.py` | ✅ |
| S1.9 | 파서 단위 테스트 (fixture md 기반, 5종) | `apps/parsers/tests/test_*_parser.py` | ✅ |
| S1.10 | sync_service 통합 테스트 (동일 파일 두 번 호출 시 두 번째 `no_change`) | `apps/sync/tests/test_sync_service.py` | ✅ |

**Phase 1 완료 기준**
- `python manage.py test apps.parsers apps.sync` 전체 통과
- `python manage.py shell` 에서 `Project` 등록 → `sync_project` 호출 → DB row 확인 수동 검증

---

## Phase 2 — 프로젝트 관리 화면

| ID | 작업 | 산출물 | 상태 |
|---|---|---|---|
| S2.1 | `templates/base.html` Tailwind 레이아웃 (사이드바 + 콘텐츠) | `templates/base.html`, `static/css/` | ⬜ |
| S2.2 | `dashboard/project_list.html` + `_project_card.html` (SCR-PROJ-01, F-PROJ-01·05) | 템플릿 + 뷰 | ⬜ |
| S2.3 | `projects/project_form.html` (SCR-PROJ-02, F-PROJ-02·03) | 폼 템플릿 + 뷰 | ⬜ |
| S2.4 | 폴더 경로 검증 (P0 항목) — 미존재/권한 없음/`docs/` 없음 분기 | `apps/projects/services/project_service.py` | ⬜ |
| S2.5 | 소프트 삭제 + 확인 모달 (F-PROJ-04) | 뷰 + 템플릿 | ⬜ |
| S2.6 | Phase 2 단위·통합 테스트 | `apps/projects/tests/` | ⬜ |

---

## Phase 3 — 상세 4개 탭

| ID | 작업 | 산출물 | 상태 |
|---|---|---|---|
| S3.1 | `_tab_nav.html` 부분 템플릿 (4개 탭 공유) | 부분 템플릿 | ⬜ |
| S3.2 | 기능 현황 탭 (SCR-PROJ-03, F-FEAT-01~04) | `features_tab.html` + 뷰 | ⬜ |
| S3.3 | Phase 진행 탭 (SCR-PROJ-04, F-PHASE-01~04) — 텍스트 완료율 | `phases_tab.html` + 뷰 | ⬜ |
| S3.4 | 프로그램 구조 탭 (SCR-PROJ-05, F-STRUCT-01~03) | `structure_tab.html` + 뷰 | ⬜ |
| S3.5 | 개선 항목 탭 (SCR-PROJ-06, F-IMPR-01~03) | `improvements_tab.html` + 뷰 | ⬜ |
| S3.6 | querystring 기반 필터 유지 검증 | 통합 테스트 | ⬜ |

---

## Phase 4 — 동기화 고도화

| ID | 작업 | 산출물 | 상태 |
|---|---|---|---|
| S4.1 | HTMX 도입 (CDN 또는 정적) + base.html 통합 | `templates/base.html` | ⬜ |
| S4.2 | 수동 동기화 버튼 + 결과 패널 (F-SYNC-01) | `sync_panel.html` + 뷰 | ⬜ |
| S4.3 | 동기화 로그 화면 (SCR-SYNC-01, F-SYNC-03) | `sync_log.html` + 뷰 | ⬜ |
| S4.4 | `python manage.py sync_all` 관리 명령어 | `apps/sync/management/commands/sync_all.py` | ⬜ |
| S4.5 | mtime 변경 감지 통합 테스트 (실제 파일 touch 후 동기화) | `apps/sync/tests/` | ⬜ |

---

## Phase 5 — E2E + 매뉴얼

| ID | 작업 | 산출물 | 상태 |
|---|---|---|---|
| S5.1 | Playwright(or Django LiveServerTestCase) 환경 구축 | `tests/e2e/` | ⬜ |
| S5.2 | 시나리오 5개 작성 (등록 → 목록 → 4개 탭 → 동기화 → 미갱신 경고) | `tests/e2e/test_*.py` | ⬜ |
| S5.3 | 화면 캡처 7장 → `docs/매뉴얼.html` | `docs/매뉴얼.html` | ⬜ |
| S5.4 | README.md 사용 가이드 보강 | `README.md` | ⬜ |

---

## 갱신 이력

| 일자 | 단계 | 변경 |
|---|---|---|
| 2026-04-29 | S0.1 ~ S0.7 | Phase 0 초기 세팅 전체 완료. Phase 1 진입 대기 |
| 2026-04-29 | S1.1 | venv 생성, Django 5.2.13 + django-environ 0.13 설치, `django-admin startproject config .` 으로 골격 생성, requirements.txt 작성. `python manage.py check` 통과 |
| 2026-04-29 | S1.2 | config/settings.py 삭제 → `config/settings/{base,dev,prod}.py` 패키지 분리, django-environ 으로 `.env` 로드. base 에 ko-kr/Asia/Seoul/templates/static 통합. manage.py 기본값 → `config.settings.dev`, wsgi/asgi 기본값 → `config.settings.prod`. dev·prod 양쪽 `manage.py check` 0 issues |
| 2026-04-29 | S1.3 | `apps/__init__.py` + 4개 앱(`projects/parsers/dashboard/sync`) `__init__.py` / `apps.py`(AppConfig, label/verbose_name) / `services/tests/__init__.py` 일괄 생성. `apps.sync.management.commands` 패키지 골격까지 포함. INSTALLED_APPS 에 `apps.<app>.apps.<App>Config` 4개 등록. 앱 레지스트리 정상 노출 |
| 2026-04-29 | S1.4 | 모델 10종 정의 — `apps/projects/models.py`(Project + ActiveProjectManager + soft_delete + 부분인덱스), `apps/dashboard/models.py`(Screen/Feature/AppModule/ModelInfo/Phase/ChecklistItem/ImprovementItem/FeatureMap), `apps/sync/models.py`(SyncLog). 모든 db_table snake_case, raw_text 보존, FK CASCADE, unique_together / 인덱스 명세대로 반영. `makemigrations --dry-run` 정상 출력(projects 1, dashboard 8, sync 1) |
| 2026-04-29 | S1.5 | `makemigrations projects dashboard sync` → 0001_initial 3종 생성. `migrate` 로 Django 기본(admin/auth/contenttypes/sessions) + 도메인 4개 마이그레이션 일괄 적용. SQLite 검증: 도메인 테이블 10종(project/screen/feature/app_module/model_info/phase/checklist_item/improvement_item/feature_map/sync_log) + 인덱스 6종(부분 인덱스 `idx_project_active WHERE deleted_at IS NULL` 포함) 정상 생성 |
| 2026-04-29 | S1.6 | `apps/parsers/services/base_parser.py` BaseParser ABC 작성 — abstract `parse(text)` / `save(project, parsed)`, `parse_and_save` 트랜잭션 래핑. `apps/parsers/services/__init__.py` 빈 PARSER_REGISTRY + 5종 placeholder 주석. 직접 인스턴스화 시 TypeError 차단 검증 |
| 2026-04-29 | S1.7 | `apps/parsers/services/{feature,structure,improvement,design,feature_map}_parser.py` 5종 구현. PARSER_REGISTRY 채움 (호출 권장 순서 design→improvement→feature→structure→feature_map). 자체 docs smoke 테스트: SCR 7 / F 22 / AppModule 4 / ModelInfo 10 / Phase 5 / ChecklistItem 21 / ImprovementItem 10 / FeatureMap 22 — 기능정의서 §1.3 수치와 정확히 일치 |
| 2026-04-29 | S1.8 | `apps/sync/services/sync_service.py` 작성 — `sync_one_file(project, filename)` (mtime 비교 → 변경 시 PARSER_REGISTRY 호출 → SyncLog 기록), `sync_project(project)` (전체 5파일 순회 + last_synced_at 갱신). 파일 누락·파서 미등록·파싱 예외 모두 SyncLog.failed 로 기록 후 계속 진행. 자체 docs 시나리오 검증: 1차 success 5 → 2차 no_change 5 → 누락 파일 failed 1 |
| 2026-04-29 | S1.9 | 파서 5종 단위 테스트 작성 — `apps/parsers/tests/test_{feature,structure,improvement,design,feature_map}_parser.py`. inline fixture md 기반. parse 순수성·추출 정확성·save DB 저장·갱신 시 기존 row 대체·raw_text 보존·빈 입력 처리·status 자동 검출·Phase 진행률 산정 검증. 30개 테스트 0.050s 전부 통과 |
| 2026-04-29 | S1.10 | `apps/sync/tests/test_sync_service.py` — 임시 docs/ 디렉토리 + Project 등록 + 5개 fixture md 기반 통합 테스트 8종. (1) 1차 sync 5건 success / (2) 2차 sync 5건 no_change / (3) 한 파일만 mtime 변조 → 변조 파일만 success 나머지 no_change / (4) 누락 파일 failed / (5) PARSER_REGISTRY 미등록 filename failed / (6) last_synced_at 갱신 / (7) DB row 영속 / (8) 한 파일 실패가 다른 파일 차단 안 함. **Phase 1 완료** — `manage.py test apps.parsers apps.sync` 총 38개 OK |
